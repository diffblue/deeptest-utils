<settings>
  <servers>
    <server>
      <id>ossrh</id>
      <username>${env.SONATYPETOKENUSER}</username>
      <password>${env.SONATYPETOKENPWD}</password>
    </server>
  </servers>
   
  <profiles>
    <profile>
      <id>ossrh</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <properties>
        <gpg.executable>gpg</gpg.executable>
        <gpg.keyname>${GPG_KEYID}</gpg.keyname>
        <gpg.passphrase>${GPG_PWD}</gpg.passphrase>
      </properties>

    </profile>
  </profiles>
</settings>
